# 環境名（通常は development または production）
RAILS_ENV=development

# Devise などで必要なキー（本番では `rails secret` で生成）
SECRET_KEY_BASE=changeme

# Admin登録用のトークン（`/users/sign_up?role=admin&admin_token=...` 用）
ADMIN_SIGNUP_TOKEN=your_admin_signup_token_here
